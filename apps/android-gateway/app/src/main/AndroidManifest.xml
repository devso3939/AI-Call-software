<?xml version="1.0" encoding="utf-8"?>
<!-- ============================================================================
     MAIN (shared) manifest — deliberately minimal.
     Every <uses-permission> lives in the FLAVOR manifests:
       src/full/AndroidManifest.xml → full SIM gateway (SMS + PHONE groups)
       src/lite/AndroidManifest.xml → OpenCall Bridge (mic + notifications only)
     The merged manifest per APK = this skeleton + one flavor overlay.
     ============================================================================ -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.OpenCall"
        android:usesCleartextTraffic="false" />

</manifest>
