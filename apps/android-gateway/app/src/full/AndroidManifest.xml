<?xml version="1.0" encoding="utf-8"?>
<!-- ============================================================================
     FULL flavor manifest — "OpenCall Gateway" (app.opencall.gateway2)

     Complete SIM gateway. Declares SMS + PHONE permission groups:
       SEND_SMS, RECEIVE_SMS          → SMS relay in/out
       CALL_PHONE, ANSWER_PHONE_CALLS → dial / answer / end cellular calls
       READ_PHONE_STATE               → call-state receiver (incoming banner)
       RECORD_AUDIO                   → audio bridge for WebRTC calls
     Note: Google Play Protect's financial-fraud prevention shows a
     pre-install warning for unregistered developers whose apps request
     SMS/PHONE permissions from browser sources — registering the app in
     the (free) Android Developer Console removes it permanently.
     ============================================================================ -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- 1.5.5: lets Telecom bind our InCallService for exact call state +
         control (mute/hold/DTMF/disconnect) when the user grants
         "Allow managing calls" (Settings → Apps → OpenCall → ⋮ or
         Special access → Call companion, API 33+). -->
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />
    <!-- 1.5.8: "Display over other apps". Apps holding it are EXEMPT from the
         Android 10+ background-activity-start ban — that is what lets the
         silent-dialer fallback (pressing Home over the stock dialer) actually
         work while the gateway runs in the background. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.OpenCall"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 1.5.14 — THE MISSING DIALER-ROLE ELIGIBILITY. Android only offers
             an app the "Default phone app" (ROLE_DIALER) role if it declares
             an ACTION_DIAL intent filter. Without it the role screen either
             never listed OpenCall or silently did nothing — the heartbeat
             kept reporting dialer:false, telecom kept the SIM call audio on
             the EARPIECE (no bound InCallService), and the far end's voice
             never reached the loudspeaker → never reached the mic → never
             reached the browser or the recording. DialDuckActivity is a
             transparent pass-through: it just forwards the dial intent to
             the stock dialer so normal "dial:" links still behave. -->
        <activity
            android:name=".DialDuckActivity"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter android:priority="-1">
                <action android:name="android.intent.action.DIAL" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <service
            android:name=".GatewayService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name=".SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- 1.5.8 FIX: the radio-level SMS send confirmation. SmsStatusReceiver
             has existed since 1.5.5 but was NEVER declared here, so the
             SMS_SENT broadcast had no target, gateway_report_sms never ran,
             and every outbound message stayed "queued" in History even after
             it actually went out. Declaring it (exported=false is fine — the
             PendingIntent broadcast is app-internal thanks to setPackage). -->
        <receiver
            android:name=".SmsStatusReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="app.opencall.gateway.SMS_SENT" />
            </intent-filter>
        </receiver>

        <!-- 1.5.6: inbound call reporting moved from a PHONE_STATE broadcast
             receiver (which was declared but never shipped — the class was
             missing, a latent crash) into FullCallService itself: the
             InCallService sees RINGING with exact number + direction, no
             broadcast timing races. -->

        <!-- 1.5.5: the InCallService binding. Telecom serves call state +
             control ONLY to services with BIND_INCALL_SERVICE. UI flag is
             false — the web app is the user interface, the stock dialer is
             auto-minimized by FullCallService itself. -->
        <service
            android:name=".FullCallService"
            android:exported="true"
            android:permission="android.permission.BIND_INCALL_SERVICE">
            <intent-filter>
                <action android:name="android.telecom.InCallService" />
            </intent-filter>
            <meta-data android:name="android.telecom.IN_CALL_SERVICE_UI" android:value="false" />
        </service>
    </application>

</manifest>
